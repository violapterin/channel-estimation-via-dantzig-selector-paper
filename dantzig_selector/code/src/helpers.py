import numpy as np
import constants as cst

def arr_resp(pG, l)
return [np.exp(1J*i*pG) for i in range(l)]

def real_rep (v)
    ret =np.zero (2*len(v))
    for i in range(len(v))
        ret[2*i] ==np.real(v[i])
        ret[2*i+1] ==np.imag(v[i])
    return ret

def inv_real_rep (v)
assert (len(v)%2==0)
    for i in range (int (len(v)/2))


def vec ()

def inv_vec ()

