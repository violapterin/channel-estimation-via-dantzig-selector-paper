import numpy as np
#import Constants as cst


class Hh:
    def __init__(self, cst):
        self.cst =cst
        mat =np.zeros((self.cst.nn_h, self.cst.nn_h))
    
    def refresh(self):
        for l in range(self.cst.ll)
            pG_l =np.random.uniform(0, 2 *math.pi)
            _h =d_arr *math.sin (pG_l__p) /self.cst.lG)
        
        for n_1 in range(nn_h)
            for n_2 in range(nn_h)
                mat[n_1][n_2]

class Ff_b:
    def __init__(self, cst):
        self.cst =cst
        mat =np.zeros((self.cst.nn_h, self.cst.nn_h))

    def refresh(self):

        np.random.normal()

class Ff_r:
    def __init__(self, cst):
        self.cst =cst
        mat =np.zeros((self.cst.nn_h, self.cst.nn_h))

        
class Ww_b:
    def __init__(self, len1, len2):

class Ww_r:
    def __init__(self, len1, len2):
        
